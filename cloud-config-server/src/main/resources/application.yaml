server:
  port: 8888

spring:
    application:
        name: config-server
    cloud:
        config:
          server:
            git: https://github:com/LEEHYUNDONG/msa-cloud-config
            search-paths: cloud-config-file/**
            default: main
